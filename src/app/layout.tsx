import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ocean Green Treinamentos - Simulados e Avaliações",
  description: "Plataforma de simulados e avaliações oficiais para Delineador Industrial.",
  keywords: ["Ocean Green", "Delineador Industrial", "Simulados", "Provas", "Avaliações"],
  authors: [{ name: "Ocean Green Treinamentos" }],
  icons: {
    icon: "/logo-ocean.jpeg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
